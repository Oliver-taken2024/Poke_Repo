@page
@model Poke_Repo.Pages.Pokemon.IndexModel

<h1>Pokemon</h1>

@foreach(var pokemon in @Model.Pokemons)
{
    <div>
        <a asp-page="/Pokemon/Details" asp-route-name="@pokemon">@pokemon</a>
    </div>
    
}